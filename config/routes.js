// import Router, { resource, model } from '@res/router'
//
// export default Router.create({
//   user: model({ ws: true, only: ['show', 'update', 'create', 'destroy'] }),
//   todo: model({ ws: true }),
//
// })
export default ({ app, io }) => {
  // console.log(app, io)
}
